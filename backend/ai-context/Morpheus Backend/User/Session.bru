meta {
  name: Session
  type: http
  seq: 1
}

post {
  url: {{url}}/auth/session
  body: json
  auth: inherit
}

body:json {
  {
    "email": "user@example.com",
    "name": "John",
    "lastName": "Doe"
  }
}

vars:pre-request {
  localhost: http://localhost:3001/api/v1
}

settings {
  encodeUrl: true
  timeout: 0
}
